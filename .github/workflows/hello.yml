name: Jenish first workflow 

on:
  push:

jobs:
  greet:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Say Hello 
        run: echo "Hello fr0m Github Actions!"
      
      - name: Print Date
        run: date
      
      - name: Show Branch 
        run: echo "Branch :${{github.ref_name}}"
     
      - name: List Files
        run: ls -la

      - name: Shows OS 
        run: uname -a 

